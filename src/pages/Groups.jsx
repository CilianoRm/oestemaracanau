import { UsersRound } from 'lucide-react'
export function Groups({data}){return <div className="page"><div className="page-top"><div><span className="eyebrow">COMUNIDADE</span><h1>Grupos</h1><p>Membros e dirigentes do serviço de campo.</p></div><div className="hero-icon"><UsersRound/></div></div><div className="group-grid">{data.groups.filter(g=>g.active).map(g=><section className="group-card" key={g.id}><div className="group-head"><div className="avatar-stack">{data.members.filter(m=>m.group_id===g.id&&m.active).slice(0,3).map(m=><span key={m.id}>{m.full_name[0]}</span>)}</div><div><b>{g.name}</b><small>{data.members.filter(m=>m.group_id===g.id&&m.active).length} membros</small></div></div><div className="member-list">{data.members.filter(m=>m.group_id===g.id&&m.active).map(m=><div className="member-row" key={m.id}><div className="avatar">{m.full_name.slice(0,1)}</div><div><b>{m.full_name}</b><span>{m.member_type==='field_leader'?'Dirigente de campo':'Publicador'}</span></div></div>)}</div></section>)}{!data.groups.length&&<div className="empty">Nenhum grupo cadastrado.</div>}</div></div>}
